<section class="max-w-md mx-auto px-4 sm:px-6 py-10">
	<div class="ui-card ui-border rounded-2xl p-6 sm:p-8">
		<p class="text-xs ui-muted tracking-widest uppercase">Hesap</p>
		<h1 class="mt-2 text-2xl font-semibold">Şifremi Unuttum</h1>
		<p class="mt-2 ui-muted">E-posta adresinizi girin, şifre sıfırlama bağlantısı gönderelim.</p>

		<% if (sent) { %>
			<div class="mt-4 border ui-border rounded-xl p-3 text-sm">
				<p class="font-semibold">İstek alındı</p>
				<p class="ui-muted mt-1">Eğer bu e-posta ile bir hesap varsa, şifre sıfırlama linki gönderildi.</p>
			</div>
		<% } %>
		<% if (hasError) { %>
			<div class="mt-4 border ui-border rounded-xl p-3 text-sm">
				<p class="font-semibold">İşlem başarısız</p>
				<p class="ui-muted mt-1">Lütfen tekrar deneyin.</p>
			</div>
		<% } %>

		<form class="mt-6 grid gap-3" method="post" action="/forgot-password">
			<label class="grid gap-1 text-sm">
				<span class="text-sm ui-muted">E-posta</span>
				<input name="email" type="email" required autocomplete="email" class="border ui-border bg-[color:var(--bg-elevated)] rounded px-3 py-2" />
			</label>

			<button type="submit" class="ui-btn ui-btn-primary w-full">Sıfırlama Linki Gönder</button>
		</form>

		<p class="mt-5 text-sm ui-muted">
			<a class="underline" href="/login">Giriş sayfasına dön</a>
		</p>
	</div>
</section>
