<section class="max-w-7xl mx-auto px-4 sm:px-6 py-10">
	<div class="flex flex-col sm:flex-row sm:items-end sm:justify-between gap-6">
		<div>
			<p class="text-xs ui-muted tracking-widest uppercase"><%= (typeof brandName === 'string' && brandName.trim()) ? brandName : 'BySamet Erkek Kuaförü' %></p>
			<h1 class="mt-2 text-3xl sm:text-4xl font-semibold">Ürünler</h1>
			<p class="mt-2 ui-muted">Tüm ürünleri filtrele, sırala ve kolayca bul.</p>
		</div>
	</div>

	<%- include('./_productList', { listPath: '/products' }) %>
</section>
